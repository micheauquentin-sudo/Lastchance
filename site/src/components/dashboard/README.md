# Dashboard démo (à venir)

Emplacement réservé pour le faux tableau de bord explorable (campagnes,
participants, statistiques…). Données factices dans un futur
`src/content/demo-data.ts`, navigation simulée côté client, aucun appel
réseau. À insérer dans `src/app/page.tsx` après le simulateur ROI.
